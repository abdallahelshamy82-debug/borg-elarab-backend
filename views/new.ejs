<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Core Energy Recharge</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@500;600;700&display=swap" rel="stylesheet">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Rajdhani', sans-serif;
        }

        html, body {
            overflow-x: hidden;
            width: 100%;
            max-width: 100vw;
        }

        body {
            background-color: #0f1016;
            color: white;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            opacity: 0;
            animation: fadeIn 0.5s ease-in forwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* --- الشريط العلوي --- */
        .navbar {
            background: rgba(21, 22, 30, 0.9);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid #2b2d3c;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .logo {
            font-size: 24px;
            font-weight: 700;
            color: #fff;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .logo span {
            color: #00ffaa;
        }

        .nav-links {
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            color: #888;
            text-decoration: none;
            font-size: 18px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: color 0.3s;
        }

        .nav-links a:hover, .nav-links a.active {
            color: #00ffaa;
        }

        /* --- Hamburger Menu --- */
        .hamburger {
            display: none;
            flex-direction: column;
            gap: 6px;
            cursor: pointer;
            z-index: 101;
            position: relative;
        }

        .hamburger span {
            width: 24px;
            height: 2px;
            background-color: #fff;
            border-radius: 2px;
            transition: all 0.3s ease;
        }

        @media (max-width: 900px) {
            .navbar {
                padding: 15px 20px;
                backdrop-filter: none;
                -webkit-backdrop-filter: none;
                flex-wrap: wrap;
            }

            .logo {
                flex-grow: 1;
            }

            .hamburger {
                display: flex;
                order: 2;
                margin-left: auto;
                justify-content: center;
                align-items: center;
                margin-top: 5px;
            }

            .nav-links {
                position: fixed;
                top: 0;
                right: -250px;
                width: 250px;
                height: 100vh;
                background: #15161e;
                flex-direction: column;
                align-items: flex-start;
                padding: 80px 30px 30px;
                box-shadow: -10px 0 30px rgba(0, 0, 0, 0.5);
                transition: right 0.4s ease;
                z-index: 100;
                border-left: 1px solid #2b2d3c;
            }

            .nav-links.active {
                right: 0;
            }

            .hamburger.active span:nth-child(1) {
                transform: translateY(8px) rotate(45deg);
                background-color: #00ffaa;
            }

            .hamburger.active span:nth-child(2) {
                opacity: 0;
            }

            .hamburger.active span:nth-child(3) {
                transform: translateY(-8px) rotate(-45deg);
                background-color: #00ffaa;
            }
        }

        .nav-links a:hover, .nav-links a.active {
            color: #00ffaa;
        }

        /* --- محتوى الصفحة --- */
        .main-container {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px 20px;
            width: 100%;
            max-width: 100vw;
            box-sizing: border-box;
        }

        .recharge-box {
            background: #15161e;
            border: 1px solid #2b2d3c;
            border-radius: 16px;
            padding: 40px;
            width: 100%;
            max-width: 500px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            position: relative;
        }

        .recharge-box::before {
            content: '';
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 4px;
            background: linear-gradient(90deg, #00ffaa, transparent);
            border-radius: 16px 16px 0 0;
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        .header h1 {
            font-size: 32px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 10px;
        }

        .header p {
            color: #888;
            font-size: 16px;
            letter-spacing: 0.5px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-box {
            background: rgba(0, 255, 170, 0.05);
            border: 1px dashed #3a3c4f;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            transition: all 0.3s;
        }

        .stat-box:hover {
            border-color: #00ffaa;
            background: rgba(0, 255, 170, 0.1);
        }

        .stat-label {
            display: block;
            color: #aaa;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 10px;
        }

        .stat-value {
            font-size: 36px;
            font-weight: 700;
            display: flex;
            align-items: baseline;
            justify-content: center;
            gap: 5px;
        }

        .points-text { color: #00ffaa; }
        .price-text { color: #fff; }

        .stat-unit {
            font-size: 16px;
            font-weight: 600;
            color: #888;
        }

        .slider-wrapper {
            margin-bottom: 40px;
        }

        /* Custom Slider */
        input[type=range] {
            -webkit-appearance: none;
            appearance: none;
            width: 100%;
            background: transparent;
            height: 30px;
            outline: none;
        }

        input[type=range]::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            height: 24px;
            width: 24px;
            border-radius: 50%;
            background: #00ffaa;
            cursor: pointer;
            margin-top: -10px;
            box-shadow: 0 0 15px rgba(0, 255, 170, 0.5);
            transition: transform 0.2s;
        }

        input[type=range]::-webkit-slider-thumb:hover {
            transform: scale(1.2);
        }

        input[type=range]::-webkit-slider-runnable-track {
            width: 100%;
            height: 4px;
            cursor: pointer;
            background: #2b2d3c;
            border-radius: 5px;
        }

        .slider-labels {
            display: flex;
            justify-content: space-between;
            color: #888;
            font-size: 14px;
            margin-top: 15px;
            font-weight: 600;
        }

        .pay-btn {
            width: 100%;
            background: #00ffaa;
            color: #0f1016;
            border: none;
            padding: 15px;
            border-radius: 8px;
            font-size: 18px;
            font-weight: 700;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }

        .pay-btn:hover {
            background: #33ffbb;
            box-shadow: 0 0 20px rgba(0, 255, 170, 0.4);
            transform: translateY(-2px);
        }

        .pay-btn.loading {
            pointer-events: none;
            opacity: 0.8;
            background: #888;
            box-shadow: none;
        }

        /* Spinner animation */
        @keyframes spin { 100% { transform: rotate(360deg); } }
        
        .spinner {
            display: none;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(15, 16, 22, 0.3);
            border-top-color: #0f1016;
            border-radius: 50%;
            animation: spin 0.8s infinite linear;
        }

        .pay-btn.loading .spinner { display: block; }

        @media (max-width: 600px) {
            .main-container { padding: 15px; }
            .recharge-box { padding: 30px 20px; border-radius: 12px; }
            .stats-grid { grid-template-columns: 1fr; gap: 15px; }
            .header h1 { font-size: 24px; }
            .stat-value { font-size: 28px; }
            .pay-btn { font-size: 14px; padding: 15px; }
        }

        /* --- Toast Notifications --- */
        #toast-container {
            position: fixed;
            bottom: 30px;
            right: 30px;
            display: flex;
            flex-direction: column;
            gap: 15px;
            z-index: 1000;
        }

        .toast {
            background: #15161e;
            color: #fff;
            border-left: 4px solid #00ffaa;
            padding: 15px 25px;
            border-radius: 8px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            font-size: 16px;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: space-between;
            min-width: 300px;
            transform: translateX(120%);
            transition: transform 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            opacity: 0;
        }

        .toast.show {
            transform: translateX(0);
            opacity: 1;
        }

        .toast.error {
            border-left-color: #ff4444;
        }

    </style>
<style>

        /* Global Mobile Responsiveness Fallback */
        html, body {
            overflow-x: hidden !important;
            max-width: 100vw !important;
            width: 100% !important;
        }
        * {
            box-sizing: border-box !important;
        }
        img, video, canvas {
            max-width: 100%;
        }
        @media (max-width: 992px) {
            .container, .admin-container, .main-container, .login-box {
                width: 100% !important;
                padding-left: 15px !important;
                padding-right: 15px !important;
                grid-template-columns: 1fr !important;
            }
            .library-grid, .grid, .stats-grid {
                grid-template-columns: 1fr !important;
            }
            .profile-sidebar, .admin-sidebar {
                width: 100% !important;
                position: relative !important;
                right: 0 !important;
                height: auto !important;
            }
            .history-table {
                display: block !important;
                overflow-x: auto !important;
                white-space: nowrap !important;
                width: 100% !important;
            }
            .toast {
                min-width: 0 !important;
                width: 90vw !important;
                right: 5vw !important;
            }
            p, h1, h2, h3, h4, h5, h6, span, div, a, td, th {
                word-wrap: break-word !important;
                overflow-wrap: break-word !important;
            }
        }

</style>
</head>
<body>

    <div id="toast-container"></div>

    <nav class="navbar">
        <div class="logo">Borg El Arab <span>Tech</span></div>
        <div class="hamburger" id="hamburgerMenu">
            <span></span>
            <span></span>
            <span></span>
        </div>
        <div class="nav-links" id="navLinks">
            <% if(user && (user.is_admin || user.role === "doctor")) { %>
            <a href="/admin/dashboard" class="nav-item" style="color: #00ffaa; border-left: 3px solid #00ffaa; padding-left: 10px;">
                [Management Panel]
            </a>
            <% } %>
            <a href="/store/ds">Dashboard</a>
            <a href="/store/ps">Store</a>
            <a href="/recharge" class="active">Recharge</a>
            <form action="/logout" method="POST" style="display: inline;">
                <button type="submit" style="background: none; border: none; color: #888; font-size: 18px; font-weight: 600; text-transform: uppercase; letter-spacing: 1px; cursor: pointer; padding: 0;">Logout</button>
            </form>
        </div>
    </nav>

    <div class="main-container">
        <div class="recharge-box">
            <% if(success) { %>
                <div class="toast success show" style="margin-bottom: 20px; position: relative; bottom: auto; right: auto; transform: none; min-width: auto; width: 100%;">
                    <span><%= success %></span>
                </div>
            <% } %>
            <% if(error) { %>
                <div class="toast error show" style="margin-bottom: 20px; position: relative; bottom: auto; right: auto; transform: none; min-width: auto; width: 100%;">
                    <span><%= error %></span>
                </div>
            <% } %>

            <div class="header">
                <h1>Core Energy</h1>
                <p>Select the amount of system points required</p>
            </div>

            <!-- Paymob Checkout Form -->
            <form action="/payment/initiate" method="POST" id="rechargeForm">
                
                <div class="stats-grid">
                    <div class="stat-box">
                        <span class="stat-label">Points Added</span>
                        <div class="stat-value points-text">
                            <span id="pointsOutput">40</span><span class="stat-unit">Pt</span>
                        </div>
                    </div>
                    <div class="stat-box">
                        <span class="stat-label">Total Cost</span>
                        <div class="stat-value price-text">
                            <span id="priceOutput">20</span><span class="stat-unit">EGP</span>
                        </div>
                    </div>
                </div>

                <div class="slider-wrapper">
                    <input type="range" name="amount" id="pointSlider" min="20" max="1000" step="20" value="40">
                    <div class="slider-labels">
                        <span>20 Pt</span>
                        <span>1000 Pt</span>
                    </div>
                </div>

                <button type="submit" class="pay-btn" id="payButton">
                    <span class="btn-text" id="btnText">Initiate Transfer</span>
                    <div class="spinner" id="btnSpinner"></div>
                </button>
            </form>
        </div>
    </div>

    <script>
        // --- Toast System ---
        function showToast(message, type = 'success') {
            const container = document.getElementById('toast-container');
            const toast = document.createElement('div');
            toast.className = `toast ${type}`;
            toast.innerHTML = `<span>${message}</span>`;
            
            container.appendChild(toast);
            
            // Trigger animation
            setTimeout(() => toast.classList.add('show'), 10);
            
            // Remove after 3s
            setTimeout(() => {
                toast.classList.remove('show');
                setTimeout(() => toast.remove(), 300);
            }, 3000);
        }

        function logoutUser() {
            // Logout is handled by the form in the navbar
        }

        // Hamburger Menu Toggle
        const hamburger = document.getElementById('hamburgerMenu');
        const navLinks = document.getElementById('navLinks');

        hamburger.addEventListener('click', () => {
            hamburger.classList.toggle('active');
            navLinks.classList.toggle('active');
        });

        // Close menu when clicking outside
        document.addEventListener('click', (e) => {
            if (!hamburger.contains(e.target) && !navLinks.contains(e.target) && navLinks.classList.contains('active')) {
                hamburger.classList.remove('active');
                navLinks.classList.remove('active');
            }
        });

        const pricePerPoint = 0.5;
        
        const pointSlider = document.getElementById('pointSlider');
        const pointsOutput = document.getElementById('pointsOutput');
        const priceOutput = document.getElementById('priceOutput');
        const payButton = document.getElementById('payButton');
        const btnText = document.getElementById('btnText');
        const btnSpinner = document.getElementById('btnSpinner');

        // Update slider track background dynamically
        function updateSliderTrack() {
            const min = parseFloat(pointSlider.min);
            const max = parseFloat(pointSlider.max);
            const val = parseFloat(pointSlider.value);
            const percentage = ((val - min) / (max - min)) * 100;
            pointSlider.style.background = `linear-gradient(to right, #00ffaa 0%, #00ffaa ${percentage}%, #2b2d3c ${percentage}%, #2b2d3c 100%)`;
        }

        pointSlider.addEventListener('input', function() {
            const points = parseInt(this.value);
            const price = points * pricePerPoint;

            pointsOutput.innerText = points;
            priceOutput.innerText = Number.isInteger(price) ? price : price.toFixed(1);

            updateSliderTrack();
        });

        // Initialize slider track on load
        updateSliderTrack();

        payButton.addEventListener('click', function() {
            payButton.classList.add('loading');
            btnText.innerText = "Processing...";
        });
    </script>

<!-- SECURITY SCRIPT -->
<style>
    body { user-select: none; -webkit-user-select: none; -moz-user-select: none; -ms-user-select: none; }
    input, textarea { user-select: text; -webkit-user-select: text; -moz-user-select: text; -ms-user-select: text; }
</style>
<script>
    document.addEventListener('contextmenu', event => event.preventDefault());
    document.addEventListener('keydown', function (event) {
        if (event.keyCode === 123) { // F12
            event.preventDefault();
        }
        if (event.ctrlKey && event.shiftKey && (event.keyCode === 73 || event.keyCode === 74)) { // Ctrl+Shift+I/J
            event.preventDefault();
        }
        if (event.ctrlKey && event.keyCode === 85) { // Ctrl+U
            event.preventDefault();
        }
    });
</script>
</body>

</html>
